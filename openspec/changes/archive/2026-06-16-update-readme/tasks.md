## 1. 本地项目 README 更新

- [x] 1.1 更新目录结构图：去掉 `.opencode/rules/` 嵌套改为 `opencode/rules/`，去掉 `setup.sh`，添加 `skills/`
- [x] 1.2 添加 `--shell` 标志用法示例到 CLI 命令部分
- [x] 1.3 更新 uninstall 说明：双重写入场景下 `.bashrc` 和 `.zshrc` 都可能包含环境变量

## 2. 测试项目 README 更新

- [x] 2.1 更新目录结构图：`opencode/rules/`（扁平）+ `skills/`（根目录），去掉 `.opencode` 嵌套
- [x] 2.2 添加 `--shell` 标志用法示例
- [x] 2.3 添加 MCP tools 说明段落（filesystem、fetch）
- [x] 2.4 添加 skills 说明段落（通用格式、跨 agent 共享、frontmatter 要求）
- [x] 2.5 推送更新到 GitHub 远程仓库
